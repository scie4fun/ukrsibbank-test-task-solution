# JPA
spring.datasource.url=jdbc:h2:mem:test;MODE=Mysql;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE;INIT=CREATE SCHEMA IF NOT EXISTS ukrsibbank_db
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.show-sql=true

# Hibernate
spring.jpa.hibernate.ddl-auto=create-drop